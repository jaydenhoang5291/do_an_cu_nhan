\subsection{Experimental Scenarios}

\textcolor{red}{Contents: i) objectives of test; ii) test scenario description: table of setting parameters for A2G; mobility of ue, number ue, number of test repetition (for statistically correct); iii) how to measure the evaluation metrics}
\subsubsection{Experiment Objectives}

The first objective is to evaluate the effect of UE/UAV altitude on the generated radio measurements. A higher aerial UE may have a higher probability of LOS connection to several gNBs, which can improve the serving-link received power but can also increase interference from neighboring cells. Therefore, altitude is expected to affect RSRP and SINR in different ways.

The second objective is to evaluate mobility and handover behavior. The simulator records the serving BS, neighboring-cell RSRP values, UE speed setting, and handover indicators. These outputs make it possible to identify when the serving BS changes, where handover events occur along the trajectory, and whether the number of handovers changes across altitude ranges.

The third objective is to evaluate protocol-level handover delay under selected delay and loss settings. This part uses handover-emulation logs to measure completion time, timeout behavior, and failure cases. The purpose is not to merge radio measurement and protocol delay into one metric, but to compare the radio-level trigger behavior with the protocol-level execution behavior.

\subsubsection{Altitude-Range Design}

The altitude cases are grouped by height intervals rather than by a single fixed UAV height. This design follows the height regimes used by the propagation models in Chapter 2 and Chapter 3. In the 3GPP UMa model, low-height terrestrial UE modelling is used for \(1.5\le h_{\mathrm{UE}}\le22.5\) m. The height-correction term in the LOS-probability expression becomes active above 13 m. For aerial vehicles, 3GPP TR 36.777 extends the UMa model to UMa-AV conditions for \(22.5<h_{\mathrm{UE}}\le300\) m, with the implemented aerial NLOS expression used up to 100 m and a LOS-dominant high-altitude branch above 100 m \cite{3gpp_38901,3gpp_36777}.

\begin{table}[H]
\centering
\caption{Altitude ranges used for A2G radio-measurement experiments}
\small
\renewcommand{\arraystretch}{1.12}
\begin{tabularx}{\textwidth}{p{2.2cm}p{3.2cm}X}
\toprule
Range & Height interval & Model interpretation\\
\midrule
H0 & \(1.5\le h_{\mathrm{UE}}\le13\) m & Ground or low terrestrial UE range under the UMa model.\\
\addlinespace[2pt]
H1 & \(13<h_{\mathrm{UE}}\le22.5\) m & Elevated terrestrial or low-height UE range under the UMa model.\\
\addlinespace[2pt]
H2 & \(22.5<h_{\mathrm{UE}}\le100\) m & Low-to-medium aerial UE range under the UMa-AV model.\\
\addlinespace[2pt]
H3 & \(100<h_{\mathrm{UE}}\le300\) m & High aerial UE range under the UMa-AV model.\\
\bottomrule
\end{tabularx}
\renewcommand{\arraystretch}{1.0}
\label{tab:altitude-ranges-a2g}
\end{table}

\subsubsection{Scenario Description}

\begin{table}[H]
\centering
\caption{Experimental scenario groups}
\small
\begin{tabularx}{\textwidth}{p{1.7cm}X X}
\toprule
Scenario & Description & Purpose\\
\midrule
S1 & Baseline terrestrial and low-height UE scenario using H0 and H1. & Provide a reference for comparing aerial UE radio behavior.\\
\addlinespace[2pt]
S2 & A2G scenario with different altitude ranges using H2 and H3. & Analyze the impact of UAV altitude on RSRP, SINR, shadow fading, serving-cell changes, and handover indicators.\\
\addlinespace[2pt]
S3 & A2G scenario with different handover-delay or transport-delay settings. & Analyze the impact of delay on handover completion time, timeout cases, and failure behavior.\\
\bottomrule
\end{tabularx}
\label{tab:experimental-scenario-groups}
\end{table}

The radio simulation settings are kept fixed across altitude scenarios except for UE height. The fixed radio parameters follow the implementation settings summarized in Table~\ref{tab:principal-radio-parameters}. This makes the altitude effect easier to interpret because the carrier frequency, gNB transmit power, antenna gains, bandwidth, noise figure, handover margin, and mobility model remain unchanged across the compared altitude groups.

\begin{table}[H]
\centering
\caption{Experiment controls for A2G simulation scenarios}
\small
\begin{tabularx}{\textwidth}{p{4.0cm}p{3.4cm}X}
\toprule
Control item & Value & Role in the experiment\\
\midrule
UE height & Ranges H0--H3 & Main variable in altitude scenarios\\
Mobility model & Manhattan mobility & UE moves along an urban road grid\\
UE speed & Fixed within each trace & Selected from the configured height-dependent range before each run\\
Simulation duration & 301 samples per trace & Output steps indexed from 0 to 300 in the generated CSV files\\
Number of UEs & 1 in the radio-measurement traces & Provides one trajectory per generated CSV file\\
Repetitions & 51--52 traces per altitude range & Reduces randomness from mobility, LOS/NLOS sampling, and shadow fading\\
\bottomrule
\end{tabularx}
\label{tab:a2g-simulation-settings}
\end{table}

For statistically meaningful comparison, each altitude range is evaluated using independently generated UE trajectories. The available radio-measurement dataset contains 52 traces for H0 and 51 traces for each of H1, H2, and H3. These repetitions reduce the effect of random mobility, LOS/NLOS sampling, and shadow fading on the reported altitude comparison.

\subsubsection{Evaluation Metrics}

The radio-level metrics are extracted from the simulator CSV output. RSRP is used to evaluate serving-cell and neighboring-cell signal strength. SINR is used to evaluate link quality under interference and thermal noise. Shadow fading is used to observe slow large-scale signal variation around the median path loss. UE speed is treated as a fixed input within each generated trace rather than as a plotted result metric. The serving BS index and handover flag are used to identify cell changes and handover events.

For each scenario, the evaluation includes both time-series and scenario-level statistics. Time-series plots show how RSRP, SINR, and shadow fading evolve along representative UE trajectories. Scenario-level plots compare the distribution and average value of RSRP and SINR across altitude ranges. Handover-related metrics are analyzed separately using serving-cell changes and protocol-level logs when available.

\subsection{Radio Measurement Results}
phân tích các chỉ số tín hiệu mà simulator xuất ra:

- RSRP
- SINR
- shadow fading
- velocity

có các biểu đồ:

- RSRP over time
- SINR over time
- Shadow fading over time
- RSRP/SINR comparison between scenarios

phân tích kiểu:

Khi UE/UAV di chuyển xa serving BS, RSRP giảm. Khi interference tăng hoặc UE gần biên cell, SINR giảm. Shadow fading làm tín hiệu dao động ngay cả khi khoảng cách thay đổi không quá lớn

This subsection analyzes the radio-measurement traces generated by the simulator for the four altitude ranges defined in Table~\ref{tab:altitude-ranges-a2g}. The analysis uses the CSV files stored in the four height-range folders. The available dataset contains 52 traces for H0 and 51 traces for each of H1, H2, and H3. Each trace contains 301 time samples for one UE trajectory. The metrics used in this subsection are RSRP, SINR, and shadow fading. UE velocity is not analyzed here because each generated trace uses a fixed speed setting, so velocity mainly acts as an input condition rather than a result metric.

\begin{figure}[!htbp]
\centering
\fbox{\parbox{0.82\textwidth}{\centering\vspace{2.2cm}
Simulator screenshot placeholder. Replace this box with a screenshot of the running radio simulator.\vspace{2.2cm}}}
\caption{Snapshot of the radio simulator during an A2G mobility scenario}
\label{fig:radio-simulator-screenshot}
\end{figure}

Table~\ref{tab:radio-measurement-summary} summarizes the measured values across all CSV samples in each altitude range. The terrestrial ranges H0 and H1 have weaker average serving-cell RSRP than the aerial ranges. The mean RSRP improves from -85.46 dBm in H0 and -78.95 dBm in H1 to -68.34 dBm in H2. This follows the expected A2G trend: when the UE height increases, the link has a higher chance of favorable propagation and the serving gNB can often be received with higher power.

\begin{table}[!htbp]
\centering
\caption{Summary of radio-measurement results across altitude ranges}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{p{1.1cm}p{2.2cm}rrrrr}
\toprule
Range & Height interval & Traces & Samples & RSRP (dBm) & SINR (dB) & Shadow fading (dB)\\
\midrule
H0 & 1.5--13 m & 52 & 15652 & -85.46 \(\pm\) 10.92 & 14.59 \(\pm\) 11.76 & -1.83 \(\pm\) 5.26\\
H1 & 13--22.5 m & 51 & 15351 & -78.95 \(\pm\) 9.71 & 11.66 \(\pm\) 9.78 & -1.78 \(\pm\) 5.21\\
H2 & 22.5--100 m & 51 & 15351 & -68.34 \(\pm\) 5.03 & 2.64 \(\pm\) 5.47 & -0.92 \(\pm\) 3.28\\
H3 & 100--300 m & 51 & 15351 & -69.92 \(\pm\) 3.32 & 0.31 \(\pm\) 3.56 & -0.12 \(\pm\) 1.45\\
\bottomrule
\end{tabular}
\renewcommand{\arraystretch}{1.0}
\label{tab:radio-measurement-summary}
\end{table}

Figure~\ref{fig:radio-rsrp-sinr-timeseries} shows representative RSRP and SINR time series. The representative trace for each altitude range is selected as the trace whose mean RSRP is closest to the median trace-level RSRP of that range. In the RSRP curves, local decreases appear when the UE moves farther from the currently serving gNB or enters a less favorable propagation condition. RSRP may recover after the serving-cell selection changes to a stronger gNB. Therefore, the RSRP curve reflects both geometric movement and serving-cell changes.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.9\textwidth]{Images/radio_rsrp_sinr_timeseries.png}
\caption{Representative RSRP and SINR time series across altitude ranges}
\label{fig:radio-rsrp-sinr-timeseries}
\end{figure}

The SINR curves show a different trend from RSRP. Although H2 and H3 have stronger average RSRP than H0 and H1, their average SINR is much lower. In Table~\ref{tab:radio-measurement-summary}, the mean SINR decreases from 14.59 dB in H0 and 11.66 dB in H1 to 2.64 dB in H2 and 0.31 dB in H3. This means that stronger received power at higher altitude does not automatically imply better link quality. At aerial heights, the UE can observe stronger signals not only from the serving gNB but also from neighboring gNBs. The increased neighboring-cell visibility raises the interference level, so SINR can decrease even when serving-cell RSRP remains high. This behavior is especially visible near cell-edge regions, where the serving and neighboring gNB powers become comparable.

Figure~\ref{fig:radio-shadow-fading-timeseries} shows the shadow-fading value of the serving link for representative traces. Shadow fading creates slow fluctuations around the median path loss and can change the received power even when the UE movement is smooth. The fluctuation range is larger in H0 and H1 than in H2 and H3. This is also reflected in Table~\ref{tab:radio-measurement-summary}, where the shadow-fading standard deviation is about 5.26 dB in H0 and 5.21 dB in H1, but decreases to 3.28 dB in H2 and 1.45 dB in H3. The high-altitude range therefore produces a more stable shadow-fading component in the generated traces.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.9\textwidth]{Images/radio_shadow_fading_timeseries.png}
\caption{Representative shadow-fading time series across altitude ranges}
\label{fig:radio-shadow-fading-timeseries}
\end{figure}

The distribution-level comparison in Figure~\ref{fig:radio-altitude-boxplots} confirms the same tendency across all traces instead of only one representative trajectory. The RSRP distribution shifts upward from the terrestrial ranges to the aerial ranges, while the SINR distribution shifts downward. The shadow-fading distribution also becomes narrower as the UE height increases, especially in H3. This confirms that altitude changes the balance between serving-link strength and interference: aerial UEs can obtain stronger received power, but they also experience stronger co-channel interference from neighboring gNBs.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\textwidth]{Images/radio_altitude_metric_boxplots.png}
\caption{Distribution of RSRP, SINR, and shadow fading across altitude ranges}
\label{fig:radio-altitude-boxplots}
\end{figure}

Figure~\ref{fig:radio-rsrp-sinr-mean-comparison} summarizes the mean RSRP and SINR values with standard-deviation bars. The result highlights the main radio-measurement conclusion of this subsection: increasing altitude improves the average serving-cell RSRP up to the low-to-medium aerial range, but it does not improve SINR. The SINR reduction in H2 and H3 indicates that interference becomes the dominant limiting factor for aerial UE links in the simulated hexagonal deployment. Consequently, RSRP and SINR must be interpreted together when evaluating A2G mobility. A handover decision based only on received power may identify a strong serving cell, but the resulting link quality can still be limited if several neighboring gNBs are also received strongly.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.82\textwidth]{Images/radio_rsrp_sinr_mean_comparison.png}
\caption{Mean RSRP and SINR comparison across altitude ranges}
\label{fig:radio-rsrp-sinr-mean-comparison}
\end{figure}

Overall, the radio-measurement results are consistent with the modelling objective of the simulator. RSRP mainly follows serving-link received power and improves when the UE has more favorable A2G propagation. SINR depends on both serving power and neighboring-cell interference, so it can degrade at higher altitude. Shadow fading introduces slow signal variation and explains why the measured radio curves are not smooth functions of distance alone.

\subsection{Handover Behavior Analysis}

The radio simulator identifies handover events from serving-BS changes. A handover indicator is recorded when the selected serving BS differs from the previous serving BS. This radio-level handover event should be interpreted as a serving-cell transition suggested by the measurement model, not as a completed protocol-level handover procedure.

The handover behavior should be evaluated using the following outputs:

\begin{itemize}
    \item serving BS over time;
    \item handover event timeline;
    \item number of handovers per altitude range;
    \item source and target BS for each handover event;
    \item comparison between handover count and altitude range.
\end{itemize}

Handover events are expected to occur mainly when the UE moves toward the boundary between two cells. In this region, the RSRP of a neighboring gNB may become higher than the RSRP of the current serving gNB by more than the configured handover margin. A higher speed can make these transitions occur over fewer simulation steps, while a higher altitude can change the number of visible candidate gNBs and the interference condition. Therefore, the handover analysis should be interpreted together with the RSRP and SINR plots rather than from the handover count alone.

For protocol-level delay analysis, the handover-emulation results are evaluated separately using trial-summary and timer-event logs. The main metrics are success rate, failure rate, timeout cases, mean successful handover delay, median successful handover delay, and 95th-percentile successful handover delay. This separation preserves the distinction between a radio-level serving-cell change and the completion of the actual handover signaling procedure.


\subsection{Measurement-Driven Handover-Delay Experiment}

The protocol-level handover experiment uses a dedicated seven-gNB measurement trace generated from the radio simulator and replayed by StormSim. The trace topology contains one center gNB and the six first-tier neighbouring gNBs of the hexagonal layout. With the simulator hexagon radius \(R=500\) m, the neighbour spacing is \(1.5R=750\) m in the horizontal direction and \(\sqrt{3}R\approx866\) m in the vertical hexagonal direction. The active rectangular region is set to \(1500\) m by \(1500\) m. Under the implemented topology generator, this region and its boundary buffer keep exactly the center cell and the first-tier neighbours, while excluding second-tier gNBs.

The generated CSV file keeps fixed RSRP columns \texttt{gnb0\_rsrp} to \texttt{gnb6\_rsrp}. The index \texttt{gnb0} represents the center gNB, and \texttt{gnb1} to \texttt{gnb6} represent the surrounding cells in angular order. The \texttt{connected\_gnb} field uses the StormSim configuration identifiers \texttt{000000} to \texttt{000006}, so that a serving-cell change in the radio trace can be replayed directly as a measurement-driven Xn handover opportunity. The StormSim configuration still contains \texttt{000007}, but this gNB is not present in the measurement trace and is not selected as a handover target in this experiment.

\begin{table}[H]
\centering
\caption{Seven-gNB StormSim measurement-trace settings}
\small
\begin{tabularx}{\textwidth}{p{4.0cm}X}
\toprule
Item & Value\\
\midrule
Measurement file & \texttt{data/stormsim\_7gnb\_measurement.csv}\\
Generator script & \texttt{generate\_stormsim\_7gnb\_data.py}\\
Topology & 1 center gNB and 6 first-tier neighbouring gNBs\\
Simulation rectangle & \(1500\) m \(\times\) \(1500\) m\\
Hexagon radius & \(500\) m\\
UE height & \(50\) m, corresponding to the H2 aerial range\\
Samples & 301 steps, indexed from 0 to 300\\
Serving-cell rule & RSRP-based selection with 3 dB handover margin\\
Detected serving-cell changes & 18 handover events\\
Handover type & Xn handover\\
Transport impairment & 0 packet loss, 0 ms latency, 0 ms jitter\\
\bottomrule
\end{tabularx}
\label{tab:stormsim-seven-gnb-settings}
\end{table}

The experiment was executed with the following StormSim command:

\begin{lstlisting}[language=bash,caption={StormSim command for the seven-gNB measurement trace}]
./bin/emulator -c config/config.yml \
  --csv-measurement data/stormsim_7gnb_measurement.csv \
  --step-delay 10
\end{lstlisting}

The reduced step delay is used only to replay the 301 measurement samples faster. The handover completion time is taken from StormSim trial summaries and timer logs, not from the CSV sample interval.

\subsection{Handover Success and Delay}

Table~\ref{tab:stormsim-seven-gnb-summary} summarizes the handover outcomes from the generated trial-summary log. All 18 detected serving-cell changes were executed as Xn handovers and completed successfully. No timeout or failure was recorded in this baseline run.

\begin{table}[H]
\centering
\caption{Seven-gNB StormSim handover outcome summary}
\begin{tabular}{lrrrrr}
\toprule
Run & Type & Trials & Success & Failure & Success rate\\
\midrule
1782627345 & Xn & 18 & 18 & 0 & 100.0\%\\
\bottomrule
\end{tabular}
\label{tab:stormsim-seven-gnb-summary}
\end{table}

The successful handover delay statistics are shown in Table~\ref{tab:stormsim-seven-gnb-delay}. The mean delay is \(200.530\) ms and the median delay is \(200.550\) ms. The minimum and maximum successful completion times are \(200.122\) ms and \(201.013\) ms, respectively.

\begin{table}[H]
\centering
\caption{Successful Xn handover delay in the seven-gNB StormSim run}
\begin{tabular}{lrrrrr}
\toprule
Type & Mean & Median & Minimum & Maximum & 95th percentile\\
\midrule
Xn & 200.530 ms & 200.550 ms & 200.122 ms & 201.013 ms & 201.013 ms\\
\bottomrule
\end{tabular}
\label{tab:stormsim-seven-gnb-delay}
\end{table}

The successful handover delays are tightly clustered around 200 ms. This does not mean that every internal handover phase takes 200 ms. Instead, the scenario waits for handover completion using a 200 ms polling ticker, so the exported trial-summary delay is quantized by the observation loop. This value is therefore suitable for comparing trial-level completion behavior in the implemented emulator, but it should not be interpreted as a precise lower-layer radio execution latency.

\subsection{Timer-Level Observation}

The timer-event log provides a finer view of the internal handover procedure. Table~\ref{tab:stormsim-seven-gnb-timers} reports successful terminal timer events from the same run. Each of the 18 Xn handover attempts produced matching start and stop records for \texttt{TXnRELOCprep}, \texttt{TXnRELOCoverall}, and \texttt{T304}. No timeout event was recorded.

\begin{table}[H]
\centering
\caption{Successful timer events in the seven-gNB StormSim run}
\begin{tabular}{lrrrr}
\toprule
Timer & Count & Mean & Median & Range\\
\midrule
\texttt{TXnRELOCprep} & 18 & 0.389 ms & 0.370 ms & 0.220--0.600 ms\\
\texttt{TXnRELOCoverall} & 18 & 0.376 ms & 0.345 ms & 0.200--0.620 ms\\
\texttt{T304} & 18 & 10.621 ms & 10.765 ms & 10.070--10.960 ms\\
\bottomrule
\end{tabular}
\label{tab:stormsim-seven-gnb-timers}
\end{table}

The timer-level data shows that internal successful timer stops occur much earlier than the 200 ms trial-summary delay. In particular, \texttt{T304} stops after about 10.6 ms on average, while the Xn relocation preparation and overall timers stop within about 1 ms. The difference between these timer values and the trial-summary delay is caused by the scenario-level success polling interval. This confirms that the timer-event log and the trial-summary log should be interpreted at different granularities: timer events describe internal procedure phases, while trial summaries describe when the scenario observes the completed handover.

\subsection{Chapter Conclusion}

This chapter defined the experimental scenario groups for radio-measurement and handover-delay analysis. The altitude design follows the height regimes used by the UMa and UMa-AV propagation models, so the A2G experiments can compare terrestrial, transition, low-to-medium aerial, and high-aerial UE behavior. The radio-measurement results show that aerial altitude improves serving-cell RSRP but can reduce SINR because neighbouring-cell interference becomes stronger. The seven-gNB StormSim run then demonstrates the measurement-driven handover workflow: 18 Xn handovers were triggered from the generated trace and all completed successfully in the baseline no-loss condition. The trial-summary delay is clustered around 200 ms because completion is observed by a 200 ms polling loop, while the timer log shows shorter internal timer durations. The next chapter summarizes the thesis, states the main limitations, and outlines future work.
